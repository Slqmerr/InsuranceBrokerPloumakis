# Form backend (app/api/quote/route.ts) — email delivery via https://resend.com
# Copy to .env.local for local development; set the same vars on Vercel for production.
# Without these, dev logs submissions to the console; production returns an error.

# API key from https://resend.com/api-keys
RESEND_API_KEY=

# Inbox that receives all form submissions (Dimitrios' email)
QUOTE_TO_EMAIL=

# Verified sender address (optional — defaults to onboarding@resend.dev,
# which works without domain verification but only for testing)
QUOTE_FROM_EMAIL=

# Keystatic CMS — the Cloud project backing the /keystatic admin panel, as
# "<team>/<project>" from https://keystatic.cloud (e.g. ploumakis/insurance).
# Not a secret: Cloud owns editor login and the GitHub connection, so the app
# itself needs no keys. Set this on Vercel and redeploy to enable Cloud login;
# leave it unset locally to edit straight into the working tree (local mode).
NEXT_PUBLIC_KEYSTATIC_CLOUD_PROJECT=
